import React from 'react';

/**
 * Presentational Container.
 * reduxに関知しない。storybookのデザイン範疇。
 * @returns {*}
 * @constructor
 */
const Preview = () => {
  return (<pre>change here</pre>);
};

export default Preview;